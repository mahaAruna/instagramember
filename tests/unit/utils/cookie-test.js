import cookie from 'emberapp/utils/cookie';
import { module, test } from 'qunit';

module('Unit | Utility | cookie', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = cookie();
    assert.ok(result);
  });
});
